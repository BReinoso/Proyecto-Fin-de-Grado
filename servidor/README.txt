Servidor programado en Flask.
Se necesita instalar flask con: $ sudo pip install Flask
Para ejecutarlo se hace con: 	$ sudo python serv.py

Hay que cambiar las rutas dentro del fichero sserv.py para que funcione, las rutas
deben pertenecer a la maquina local.